{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal capabilities for Ctrl-Custo desktop — no local filesystem or shell access",
  "windows": ["main"],
  "permissions": []
}
